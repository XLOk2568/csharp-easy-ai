<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFtransformer.Pages.child"
        xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf" xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern" x:Class="WPFtransformer.Pages.child.GuaGuaCF"
        mc:Ignorable="d"
        Title="GuaGua" Height="450" Width="800" Loaded="Window_Loaded" Closed="Window_Closed"     
    WindowStyle="None"   
    AllowsTransparency="True"      
    Background="#FF272727"
    SourceInitialized="Window_SourceInitialized"
     >
    <Border x:Name="RootBorder"
          BorderThickness="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <!-- 标题栏 -->
                <RowDefinition Height="*"/>
                <!-- 内容区 -->
            </Grid.RowDefinitions>
            <!-- 标题栏 -->
            <Grid Grid.Row="0"
            Background="#FF202020"
            MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock Text="特征选择"
                   VerticalAlignment="Center"
                   Margin="12,0,0,0"
                   FontSize="14"
                   Foreground="White"/>
                <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="0,0,8,0">
                    <Button Click="Minimize_Click" Content="🗕" Background="#FF202020" Foreground="#FFD6D6D6" BorderBrush="#FF202020"/>
                    <Button Click="MaxRestore_Click" Content="□" Background="#FF202020" Foreground="#FFD6D6D6" BorderBrush="#FF202020"/>
                    <Button Click="Close_Click" Content="✕" Background="#FF202020" Foreground="#FFD6D6D6" BorderBrush="#FF202020"/>
                </StackPanel>
            </Grid>
            <!-- 主体内容 -->
            <Grid Grid.Row="1" Background="Transparent">
                <Button Content="分别选择需要提取特征的图片与保存特征的文件夹" Margin="10,10,0,0" VerticalAlignment="Top" Click="Button_Click"/>
                <!-- 放置其他控件 -->
                <Canvas Margin="0,400,0,0" HorizontalAlignment="Center" VerticalAlignment="Bottom" Width="168" Height="58" Cursor="">
                    <ui:ProgressRing x:Name="ring" IsActive="True" Height="38" Width="38"  Visibility="Visible" HorizontalAlignment="Left" Canvas.Left="66"/>
                </Canvas>
            </Grid>
        </Grid>
    </Border>
</Window>
